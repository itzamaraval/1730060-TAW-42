<div class="text-nowrap text-md-right">
    <a href="{{ route('ejemplos.show', $ejemplo->id) }}" class="btn {{ !request()->ajax() ? 'btn-primary' : 'btn-link text-secondary p-1' }}" title="{{ __('Show') }}">
        <i class="far fa-eye {{ !request()->ajax() ? 'fa-fw' : '' }}"></i>
    </a>

    <a href="{{ route('ejemplos.edit', $ejemplo->id) }}" class="btn {{ !request()->ajax() ? 'btn-primary' : 'btn-link text-secondary p-1' }}" title="{{ __('Edit') }}">
        <i class="far fa-edit {{ !request()->ajax() ? 'fa-fw' : '' }}"></i>
    </a>

    <a href="{{ route('ejemplos.destroy', $ejemplo->id) }}" class="btn {{ !request()->ajax() ? 'btn-primary' : 'btn-link text-secondary p-1' }}" title="{{ __('Delete') }}"
       onclick="event.preventDefault(); if (confirm('{{ __('Delete This Ejemplo?') }}')) $('#delete_ejemplo_{{ $ejemplo->id }}_form').submit();">
        <i class="far fa-trash-alt {{ !request()->ajax() ? 'fa-fw' : '' }}"></i>
    </a>

    <form method="post" action="{{ route('ejemplos.destroy', $ejemplo->id) }}" id="delete_ejemplo_{{ $ejemplo->id }}_form" class="d-none">
        @csrf
        @method('delete')
    </form>
</div>
